# 簡易版 .htaccess (500エラー対処用)
# 問題がある場合は段階的に有効化

# 基本設定のみ
AddDefaultCharset UTF-8

# ディレクトリブラウジング無効化
Options -Indexes

# 基本的なセキュリティファイル保護
<Files ".env">
    Order allow,deny
    Deny from all
</Files>

<Files "*.log">
    Order allow,deny
    Deny from all
</Files>

# 段階的に以下をコメントアウト解除してテスト

# # Apache Rewrite Engine (段階2で有効化)
# RewriteEngine On

# # 基本的なAPI ルーティング (段階3で有効化)
# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteRule ^api/dashboard/data/?$ api/dashboard/data.php [QSA,L]

# # 管理画面ルーティング (段階4で有効化)  
# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteRule ^dashboard/?$ index.php [QSA,L]

# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteRule ^clients/?$ clients.php [QSA,L]